apiVersion: batch/v1
kind: CronJob
metadata:
  name: ba-data-process-job
spec:
  schedule: "0 3 * * *"
  jobTemplate:
    spec:
      completions: 1
      template:
        metadata:
          labels:
            app: ba-data-process
        spec:
          restartPolicy: Never
          imagePullSecrets:
            - name: ghcr-secret
          containers:
            - name: ba-data-process
              image: ghcr.io/beaverhouse/ba-data-process:4986365b
              imagePullPolicy: IfNotPresent
              envFrom:
                - secretRef:
                    name: ba-worker-secret
              env:
                - name: GO_ENV
                  value: "production"
